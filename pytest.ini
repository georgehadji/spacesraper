[pytest]
asyncio_mode = auto
# CI invokes bare `pytest`, which does not put the working directory on
# sys.path the way `python -m pytest` does — so tests/conftest.py's
# `from src...` import failed there while passing locally. Setting it here
# fixes both entry points and both workflows in one place.
pythonpath = .
